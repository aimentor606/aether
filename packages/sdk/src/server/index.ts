/**
 * @aether/sdk/server — Server-side utilities (Node.js only, no React).
 *
 * Import from '@aether/sdk/server' in API routes, server actions, middleware.
 */

export { createSupabaseAdminClient } from './supabase-admin';
